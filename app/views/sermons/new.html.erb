<h1>New Sermon</h1>

<%= form_for @sermon, html: { multipart: true } do |f| %>
	<p>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :pastor %>
		<%= f.text_field :pastor %>
	</p>
	<p>
		<%= f.file_field :audio %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>